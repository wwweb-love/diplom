import styled from "styled-components"

const AdminProductCardContainer = ({ className }) => {
    return (
        <div className={className}>
            AdminProductCard
        </div>
    )
}

export const AdminProductCard = styled(AdminProductCardContainer)``