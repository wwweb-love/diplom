import styled from "styled-components"

const BasketCardContainer = ({ className }) => {
    return (
        <div className={className}>
            BasketCard
        </div>
    )
}

export const BasketCard = styled(BasketCardContainer)``